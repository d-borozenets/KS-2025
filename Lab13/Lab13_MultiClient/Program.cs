using System.Net;
using System.Net.Sockets;
using System.Text;

class Client{
    static void Main(){
        const int port = 5000;
        const string host = "127.0.0.1";
    }

    private static void ReadResponse(/*тут щось є*/){
        byte[] buffer = new byte[1024];
        while (true ) {//TODO: заміти true на потрібне значення
            try{
                
            }
            catch(IOException){ break; }
        }
    }
}
